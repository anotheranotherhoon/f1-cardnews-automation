# 후니스피드 카드뉴스 명세

**이 문서가 단일 출처다.** 카드 구성·문구 규칙을 바꿀 때는 여기를 먼저 고치고, 그다음
`workflows/build-wf1.js`(카드 데이터)와 `templates/`(레이아웃)에 반영한다.

텔레그램으로 보내는 수정 요청은 **그 실행에만 적용되는 1회성**이다. 매주 반복되어야 하는
요구사항은 반드시 이 문서에 적고 코드에 반영해야 한다. 이 구분을 놓쳐서 요구사항이 사라진 적이 있다.

---

## 공통 규칙

### 표지(카드 1) 문구 — 5일 동일 형식

```
제목    대회 공식 영어명            예: Hungarian Grand Prix
부제    한글 설명                   예: 퀄리파잉 · 폴 Lando Norris
```

한 줄 안에 영어와 한글을 섞지 않는다. 대회명은 항상 영어 공식명칭을 쓴다
(초기 결정: "공식명칭을 따르고 싶어"). 연도·라운드는 상단 킥커에 이미 있으므로 제목에 넣지 않는다.

**부제 한 줄에는 세션 이름과 그 결과를 함께 담는다.** 세션명만 있는 줄과 결과만 있는 줄을
따로 두면 어느 세션의 결과인지 모호해진다. 부제가 길어지면 `\n`으로 줄을 나눈다
(렌더러가 `<br>`로 바꾼다). 두 세션을 다루는 날은 한 줄에 하나씩 배치한다.

```
잘못됨                          올바름
British Grand Prix             British Grand Prix
스프린트 & 퀄리파잉               스프린트 우승 Andrea Kimi Antonelli
우승 Andrea Kimi Antonelli      폴 포지션 Lewis Hamilton
```

왼쪽은 퀄리파잉도 그가 1위한 것처럼 읽힌다. 실제로는 폴이 해밀턴이었다.

### 이슈 카드는 주제별로 블록을 나눈다

**한 문단에 여러 주제를 몰지 않는다.** 프리뷰·스프린트·총정리 **모든 브랜치**에 적용된다.
`issue-blocks` 템플릿이 `issues: [{head, body}]` 배열을 받아 주제별 블록으로 렌더한다.
`head`는 12자 이내, `body`는 60자 이내로 하고, 서로 다른 주제로 2~4개를 만든다.

프롬프트에 **"한 항목에 여러 주제를 섞지 말 것"**을 명시한다. 이걸 빼면 모델이 접속사로
이어붙인 한 덩어리를 돌려준다. 2026-08-20 프리뷰에서 잔트보르트 캘린더 제외와 하샤르 부상이
한 문단에 섞여 나갔다 — 규칙은 월요일 항목에만 적혀 있었고 프리뷰는 `issueSummary`라는
단일 문자열을 쓰고 있었다.

배경 사진이 없으면 서킷 배경 자산으로 폴백한다 (이미지 출처 우선순위 2순위).

### 순위 카드는 1등부터 꼴등까지 전원을 노출한다

**상위 N명만 자르지 않는다.** 레이스 결과, 드라이버 순위, 컨스트럭터 순위, 레이스 그리드가 해당한다.
(연습주행·Q3처럼 애초에 상위권만 의미 있는 카드는 예외다.)

인원수는 데이터에서 센다. 2026 시즌은 22명이지만 2025 시즌 데이터를 넣으면 20명이 나와야 한다.
`slice(0, 20)` 같은 하드코딩을 금지한다. 2열 분할은 `Math.ceil(길이 / 2)`로 계산하고,
구간 라벨(P16–P22 등)도 실제 순위에서 뽑는다.

### 컨스트럭터 순위는 한 열로 세로 나열한다

11개 팀이라 2열로 나누지 않는다. `singleColumn: true` 로 한 열 전체 폭을 쓰고 행을 크게 잡는다.
팀명은 줄이지 않는다 (`shortenNames: false`) — "Aston Martin"이 "A. Martin"이 되면 안 된다.

### 순위 카드에는 등락을 표시한다

드라이버·컨스트럭터 순위는 직전 라운드 대비 등락(▲▼)을 함께 보여준다.
행에 `team`을 넘겨 팀 컬러 바도 적용한다 — 빠뜨리면 전부 회색으로 나온다.

### 스타팅 그리드는 실제 그리드처럼 지그재그로 배치한다

일렬로 정렬하지 않는다. 홀수 그리드는 왼쪽 열, 짝수는 오른쪽 열에 넣고 오른쪽 열을 반 칸 아래로
밀어 어긋나게 한다 — 그리드를 위에서 내려다본 모습이 된다. 상단에 체커 패턴 출발선을 두어
어느 쪽이 앞인지 보이게 하고, 두 열 사이에 점선 중앙선을 깐다. 폴 포지션만 빨간색으로 강조한다.
좁은 열에 맞춰 이름은 `L. Norris` 형태로 줄이되 팀 컬러 바는 유지한다.

레이스 그리드와 스프린트 그리드 모두 같은 템플릿(`grid.html`)을 쓴다.

### 브랜드 표기

`hooni.speed` — 항상 소문자. `hooni`는 흰색, `.speed`는 빨강(#e10600).

### 이미지 출처 우선순위

| 용도 | 1순위 | 2순위 |
|---|---|---|
| 이슈 카드 배경 | Pexels 분위기 사진 (장식용) | 서킷 배경 자산 |
| DOTD | 드라이버 액션 캐릭터 (`assets/dotd/`) | 텍스트만 |
| 표지·스토리 | 주간 생성 이미지 | 단색 |

**사진은 "이 장면이다"라고 주장하지 않는 자리에만 쓴다.** 다른 경기 사진을 특정 사건 옆에 두면
라이선스가 깨끗해도 사실관계가 틀린 게시물이 된다. Pexels 검색어에 팀명·대회명·드라이버명 금지.

---

## 요일별 구성

요일이 아니라 **실제 세션 종료 시각**으로 판별한다 (라스베이거스처럼 레이스가 토요일 밤인 경우 대응).

### 목 — 프리뷰 (`preview`)

```
1  표지      Hungarian Grand Prix / 프리뷰 · 레이스 7월 26일 (일) 22:00 KST
2  트랙      길이 · 랩 수 · 위치 · 설명
3  타이어    작년 vs 올해 컴파운드
4  이슈      작년 해당 GP의 주요 사건 (주제별 블록 2~3개, issue-blocks)
5  포디움    작년 시상대
+  스토리    밈 이미지 (9:16)
```

### 금 — 관전 가이드 (`guide`)

```
1  표지      Hungarian Grand Prix / 주말 관전 가이드
2  시간표    전 세션 KST, 퀄리·레이스 강조
3  라인업    팀별 드라이버
```

한국 시간 기준으로 금요일 아침에는 아직 세션이 열리지 않았다. 그래서 결과가 아니라 예고를 싣는다.

### 토 — 연습주행 (`practice-results` / 스프린트 주간 `sprint-fri-results`)

```
1  표지      Hungarian Grand Prix / 연습주행 FP1 · FP2 결과
2  FP1       상위 10명
3  FP2       상위 10명
```

스프린트 주간에는 FP2 대신 스프린트 퀄리파잉이 들어가고, 스프린트 그리드 카드가 추가된다.

일요일 스프린트 브랜치(`sprint-sat-results`)도 이슈 카드에 Gemini 리서치 + Pexels 배경을 쓰고,
드라이버 순위는 전원을 노출한다 — 월요일만 고치고 여기를 빠뜨린 적이 있다.

### 일 — 퀄리파잉 (`quali-results` / 스프린트 주간 `sprint-sat-results`)

```
1  표지      Hungarian Grand Prix / 퀄리파잉 · 폴 Lando Norris
2  Q3        상위 10명
3  탈락 구간  Q2 탈락 · Q1 탈락 (라벨은 실제 순위에서 계산)
4  그리드    전원 (2열)
```

### 월 — 총정리 (`race-recap`)

```
1  표지      Hungarian Grand Prix / [Gemini 후킹 문구 16자 이내]
2  레이스 결과  전원 (2열) · 완주는 포인트, 리타이어는 RET
3  이슈      주제별 블록 2~4개 + Pexels 배경 + Fastest Lap
4  DOTD      수상자 · 팀 · 선정 이유 + 액션 캐릭터
5  타이어 전략  상위 5명 스틴트
6  드라이버 순위  전원 (2열)
7  컨스트럭터 순위  전 팀
+  스토리    포디움 이미지 (9:16)
```

**월요일 확정 요구사항** (2026-08-17):

- 카드 1은 후킹 문구가 있어야 한다. 사실 나열로 끝내지 않는다.
- 카드 2는 22위까지 전부 보여준다.
- 카드 3은 한 문단에 여러 주제를 몰지 않는다. 주제별로 나누고 각 60자 이내.
- 카드 4는 단독 주제이므로 이미지를 반드시 넣는다.
- 카드 6은 22위까지 전부 보여준다.

완주 판정: Jolpica `status`가 `Finished` · `Lapped` · `+N Lap`이면 완주. 그 외만 `RET`.
`Lapped`를 리타이어로 잘못 분류해 8위 이하가 전부 RET로 표시된 적이 있다.

---

## 월요일 스토리 — 포디움 이미지

**세 명을 함께 그린다.** 우승자 혼자가 아니다 (2026-08-17 확정).

```
구도   P1  가운데 가장 높은 단, 가장 크게, 빛도 가장 밝게 — 주인공이 명확해야 한다
       P2  왼쪽 낮은 단, 조금 작고 뒤쪽
       P3  오른쪽 낮은 단, 조금 작고 뒤쪽
```

**샴페인은 옅은 황금빛 호박색(pale golden amber)으로 그린다.** 반투명하고 반짝이는 실제 샴페인 색이어야 한다.
상아색·크림색·불투명한 흰색은 금지 — 흰 페인트처럼 보인다는 지적이 있었다. 병은 진한 초록 유리.

순위(1·2·3)와 이름·기록은 **HTML 오버레이로 넣는다.** 그림 속 숫자에 의존하면 AI가 틀리게 그렸을 때
잘못된 정보가 나간다. 템플릿은 `story-podium-full.html`.

이미지에는 숫자·글자·로고를 넣지 않는다. 인물과 포디움은 상단 60% 안에 배치하고, 하단은 같은 장면이
자연스럽게 이어지게 그린다(자막 오버레이가 덮음).

## DOTD 캐릭터 자산

`assets/dotd/<코드>/<포즈>.png` — 라운드 번호로 나눠 매주 다른 포즈를 쓴다.

```
5장   lec ham rus ant ver
3장   nor pia
1장   나머지 15명
```

포즈 풀: `fist point arms-folded thumbs kiss v-one v-two salute heart shhh punch`

**T포즈(`tpose`)는 러셀 전용이다.** 다른 드라이버에게 배정하지 않는다.

서버 배포 경로는 `/data/cards/dotd/`이며 `https://xrp-admin.p-e.kr/hooni-cards/dotd/`로 서빙된다.
렌더러는 이 경로를 직접 읽어 포즈를 고르므로, 자산을 추가하면 코드 수정 없이 반영된다.

### 자산이 없는 드라이버 (2026-08-20 추가)

부상 교체 등으로 로스터에 없는 드라이버가 포디움이나 DOTD에 들면 캐릭터 자산이 없다.
그대로 두면 `usePodium`이 꺼져 **포디움 스토리가 조용히 일반 스토리로 대체된다** — 명세에
확정한 카드가 경고 없이 사라진다. 그래서 WF-1에 승인 게이트를 뒀다 (월 총정리 전용).

```
RC Cards → Detect Missing Assets → 누락 없으면 평소대로
                                 → 있으면 [🎨 생성] [📝 텍스트로]  (6시간)
   생성 → 공식 포트레이트 수집 → 캐릭터 생성 → [✅ 사용] [🗑 버림]  (6시간)
   ✅ 만 characters-v2/<코드>/base.png 로 저장된다
```

- **판정 대상은 포디움 3인과 DOTD 수상자뿐이다.** 4위 이하는 캐릭터를 쓰지 않으므로 묻지 않는다.
- **감지 실패는 fail-open이다.** 예외가 나면 게이트를 건너뛰고 정상 발행한다 — 게이트 버그로
  월요일 발행이 멈추는 것이 최악이다.
- **한 번에 한 명만 생성한다.** 여러 명이면 메시지에 알리고 첫 명만 처리한다. n8n HTTP 노드는
  아이템 수만큼 반복 실행되므로 여러 명을 한 번에 돌리면 레이트리밋에 걸린다.
- **무응답·거절·생성실패는 모두 텍스트 스토리로 간다.** 되돌릴 수 없는 실패가 아니다 —
  나중에 자산을 준비하고 발행 완료 알림의 🔄 다시 만들기를 누르면 다시 만들 수 있다.
- 프리뷰는 게이트를 타지 않는다. 밈 표지의 드라이버는 WF-6 안에서 LLM이 정하므로 이 시점에
  알 수 없고, 참조가 없어도 인물 오표기가 아니라 화풍 흔들림에 그친다.
- 실물 특징 설명(`DRIVERS[code]`)이 없는 신규 드라이버는 이름만으로 그리면 남 얼굴이 나온다.
  그래서 F1 공식 포트레이트를 참조로 넣는다. 없으면(신인) 참조 없이 생성하고 2단 확인에 맡긴다.

### 생성된 자산은 서버에만 남는다

**n8n은 git을 모른다.** 게이트가 만든 캐릭터는 서버 한 곳에만 존재하고, 저장소·GitHub에는
반영되지 않는다. 프리티어 VM 하나뿐이므로 백업이 없는 상태다.

```
python3 workflows/sync-assets.py            # 서버에 새로 생긴 자산 확인
python3 workflows/sync-assets.py --apply    # 내려받기 → 커밋 → 푸시
```

**내용이 다른 파일은 기본적으로 가져오지 않는다.** dotd는 저장소가 원본(약 2MB),
서버가 서빙용 경량화 사본(약 0.8MB)이라 그냥 당겨오면 원본이 압축본으로 덮어써진다.
가져와야 할 것은 "서버에서 새로 생성된 자산"뿐이다.

`meme/`은 라운드별 스토리 산출물이라 동기화 대상이 아니다 (매 경기 재생성된다).

**맥에서 `scp -r` 로 자산을 올리면 `._base.png` 같은 AppleDouble 부산물이 함께 올라간다.**
`.png`로 끝나고 정렬에서 맨 앞이라, DOTD 포즈 선택에 섞이면 깨진 이미지가 카드에 들어간다.
포즈 필터와 자산 보유 판정에서 `.`으로 시작하는 파일을 제외하지만, 올리고 나서
`find <경로> -name '._*' -delete` 로 정리하는 습관이 낫다.

### 캐릭터 생성 규칙

- 화풍은 `gen-assets.js`의 `STYLE_CORE`가 단일 출처다. 구도만 용도별로 다르다
  (`STYLE_V2` 전신 / `STYLE_V2_BUST` 가슴 위).
- 스폰서 로고가 반복적으로 새어 나온다. 특히 **빨강(페라리) 수트**가 가장 심하다.
  금지 문구만 강화하는 방식은 네 번 실패했다 — v2 캐릭터를 참조로 써도 실패한다.
  **효과가 있었던 것은 가슴을 디자인으로 채우는 방식**이다: 왼쪽 어깨에서 오른쪽 허리로 내려오는
  대각선 스트라이프 하나를 넣으면 모델이 로고를 넣을 빈 공간이 없어진다. 그래도 완전히
  사라지지는 않으니, 생성 후 확인하고 남은 것은 카드에서 20px 남짓으로 보이므로 그대로 둔다.
- v2 캐릭터(`assets/characters-v2/`, 22명)의 base는 로고가 없다. 포디움 스토리 참조는 이걸 쓴다.
- 원본 파일을 덮어쓰는 i2i 편집을 하지 않는다. 과거에 포디움 그림을 이 방식으로 망친 적이 있다.

---

## 문제가 생겼을 때 볼 곳

| 증상 | 확인할 것 |
|---|---|
| 이슈·DOTD·후킹 문구가 빔 | `/data/hooni_speed/last-research.json` — Gemini 리서치 응답 원문과 실패 이유 |
| 승인 요청이 안 옴 | `/data/hooni_speed/pending-approval.json` 의 **갱신 시각**. 파일 존재만 보면 이전 실행 것을 새 것으로 오인한다 |
| 카드가 반영 안 된 것처럼 보임 | 텔레그램 메시지의 `🆔 <폴더명>` 으로 세트를 구분한다. 여러 세트가 쌓여 있다 |
| 알림이 어느 요청 것인지 모름 | 모든 알림에 `🆔` 가 붙어야 한다. 없으면 그 노드에 추가한다 |
| 텔레그램 전송이 `Bad request` | 일시적 거부. URL·이미지는 정상인 경우가 많다. Send Cards에 재시도 3회가 붙어 있다 |
| 렌더 중 파일이 사라짐 | 폴더명에 실행 식별자가 붙는지 확인. 없으면 만료된 옛 실행의 Cleanup이 지운다 |

### 리서치 프롬프트 주의

"확실하지 않은 정보는 null" 로만 지시하면 모델이 **응답 전체를 `null`로** 반환한다.
반드시 "최상위는 위 키들을 가진 JSON 객체여야 하며 응답 전체를 null로 출력하지 말 것,
못 찾은 개별 필드만 null, issues를 못 찾으면 빈 배열" 을 명시한다.
파서도 `text === 'null'` 을 걸러 진단 파일에 이유를 남긴다.

리서치·Pexels 노드는 `onError: continueRegularOutput`이다 — 실패해도 나머지 카드는 발행되어야 하기
때문이다. 대신 실패가 조용히 묻히므로 진단 파일을 반드시 함께 남긴다.

**리서치 브랜치는 세 개다. 고칠 때 셋 다 고친다.** `PV`(목 프리뷰) · `SS`(스프린트 토) · `RC`(월 총정리).
2026-08-20 네덜란드 GP 프리뷰에서 타이어·이슈 카드가 빈 채로 발행됐다. 위의 null 방어 문구와
진단 파일 기록이 `SS`·`RC`에만 적용되어 있었고 `PV`는 옛 프롬프트("확실하지 않은 정보는 null로.")
그대로였다. 진단 파일도 안 남겨서 `last-research.json`이 사흘 전 것이었고, 원인 파악이 늦어졌다.
브랜치가 여러 개인 수정은 `grep`으로 전 브랜치를 확인하고 끝낸다.

### 리서치 재시도 (2026-08-20 추가)

**HTTP 노드의 `retryOnFail`은 의미상 실패를 잡지 못한다.** 모델이 200 OK로 최상위 `null`이나
깨진 JSON을 돌려주면 노드는 성공으로 통과하고, 실패는 다음 Code 노드에서야 드러난다.
그 지점에는 재시도가 없었다 — 위 사고 당시 재시도 설정이 있었는데도 한 번도 돌지 않은 이유다.

n8n에는 루프가 없으므로 `build-wf1.js`의 `researchChain()`이 시도 횟수만큼 노드를 펼친다.

```
Research → Parse 1 → Retry? 1 ─실패→ Research 2 → Parse 2 → Retry? 2 ─실패→ Research 3 → Parse 3 ─┐
                        └─성공────────────────────────┴─성공─────────────────────────────────────┴→ Merge Research
```

- 시도 횟수와 temperature는 `RESEARCH_TEMPS`(현재 `[0.1, 0.45, 0.8]`) 하나로 조절한다.
  배열 길이를 바꾸면 노드가 그만큼 늘어난다.
- 재시도에는 temperature를 올리고 형식 지시문을 덧붙인다 — 같은 프롬프트로 같은 온도면
  같은 실패가 반복된다.
- 마지막 시도는 성공·실패 무관하게 `Merge Research`로 간다. **발행은 계속되어야 한다**는
  기존 원칙은 유지한다.
- **프롬프트에서 `$json`을 쓰지 않는다.** 2회차부터 입력이 IF 노드 출력이라 `$json.season` 같은
  값이 사라진다. 반드시 `$('PV Extract')`처럼 노드를 명시해 참조한다.
- 진단 파일에 `attempts`가 남으므로 몇 번째에 성공했는지 확인할 수 있다.

재시도는 일시적 실패(타임아웃·쿼터·그라운딩 실패)에만 듣는다. 프롬프트 결함성 실패는
몇 번을 돌려도 같은 결과이므로, `attempts: 3`이고 `ok: false`면 프롬프트를 의심한다.

## 빈 카드 방어 (2026-08-20 추가)

**`needsReview`와 `emptyCards`는 다르다.**

| 신호 | 조건 | 표시 |
|---|---|---|
| `needsReview` | 카드에 `needsLlm` 선언이 있음 (고정값) | `⚠️ 검수 필요(LLM 생성)` |
| `emptyCards` | **본문이 실제로 비었음** | `🛑 본문 없음` + 안내 메시지에 카드 번호 요약 |

`needsLlm`은 "이 카드는 LLM을 쓴다"는 선언이라 성공·실패와 무관하게 항상 켜진다. 프리뷰면
카드 1·3·4에 매번 `⚠️`가 붙어서 신호 역할을 못 했고, 그래서 빈 카드가 승인을 통과했다.
판정은 `build-wf2.js`의 `isBodyEmpty()`가 카드 **데이터**로 한다 — 렌더된 HTML 문자열을 검사하지 않는다.

새 템플릿을 추가하면 `isBodyEmpty()`에 판정 규칙도 함께 넣는다. 빠뜨리면 조용히 `false`가 되어
빈 카드가 경고 없이 지나간다.

**표지는 빈 카드로 보지 않는다.** LLM 배경 생성이 실패해도 제목·시각은 데이터에서 들어오므로
카드가 성립한다.

### tires 카드는 산문이다

`case 'tires'`가 `result-full`(순위표, `d.rows`를 읽음)로 폴스루되어 있어서, `PV Cards`가 넘기는
`body`가 통째로 무시됐다. **리서치가 성공해도 빈 카드가 나왔다.** `text-card` 그룹으로 옮겼다.
`titles` 맵에 `tires`가 이미 있던 것이 원래 의도가 `text-card`였다는 증거다.

## 발행 후 재요청 (2026-08-20 추가)

빈 카드를 승인해도 되돌릴 방법이 없었다. 발행 직후 `Cleanup Cards`가 렌더된 PNG를 지우기 때문에
같은 URL로 다시 올릴 수도 없고, WF-1 실행도 이미 끝나 재요청 창구가 없었다.

그래서 **발행 완료 알림에 `🔄 다시 만들기` 버튼**을 붙였다. 누르면 같은 회차를 처음부터 다시 만들어
평소와 똑같은 승인 요청이 온다.

```
WF-4 Cleanup Cards → last-publish.json 기록 {dayType, round, season, publishedAt}
WF-4 Notify Success → 버튼 (WF-1 재요청 웹훅 GET, ?k=<키>)
WF-1 Republish Webhook → IF Republish Auth → Load Last Publish → Jolpica Calendar → 평소 경로 합류
```

- **버튼 URL에 회차를 싣지 않는다.** `last-publish.json`에서 읽으므로 링크를 재사용해도 항상
  최근 발행분만 다시 만들어진다.
- **12시간 제한.** 옛 링크로 지난 회차를 다시 올리는 사고를 막는다.
- 웹훅은 `responseMode: onReceived`다 — 파이프라인이 12분 이상 걸려서 즉시 응답해야 한다.
  **따라서 HTTP 200은 인증 통과를 뜻하지 않는다.** 키 검증은 IF 노드에서 하고, 실패하면
  조용히 중단한다(알림 없음). 동작 확인은 실행 이력으로 한다.
- **이미 올라간 인스타 게시물은 API로 지울 수 없다.** 앱에서 직접 삭제해야 하며, 알림 문구에
  이 안내가 들어 있다.
- `Resolve Day Type`의 `const OVERRIDE = null;` **한 줄은 글자 그대로 유지한다.**
  `test-wf1.py`·`test-branches.py`가 문자열 치환에, `sim-weekend.py`가 `assert`에 쓴다.
  재요청 값은 별도 변수로 받아 `const EFF = OVERRIDE || REPUBLISH` 로 합친다.

## 텔레그램 알림 규칙

**모든 알림에 `🆔 <dirName> · <raceName>` 을 붙인다.** 승인 요청에만 있으면, 그 뒤에 오는
승인·만료·초과·발행완료·발행실패 알림이 어느 요청에 대한 응답인지 알 수 없다. 발행 주기가
주 5회이고 재요청까지 가능하므로 같은 GP의 여러 세트가 대화에 섞인다.

대상 노드: `Ask In Chat` · `Notify Publishing` · `Notify Expired` · `Notify Max`(WF-3),
`Notify Success` · `Notify Failure`(WF-4). 알림 노드를 새로 만들면 여기에 추가한다.

**취소와 만료는 다른 문구를 쓴다.** 두 경로(`Switch Decision` 출력 1·3)가 같은 `Notify Expired`
노드로 들어오므로 `decision === 'cancel'` 로 분기한다. 사용자가 직접 취소했는데
"승인 없이 만료되어"라고 오면 요청이 유실된 것처럼 읽힌다.

## 운영 제약

### 승인 대기 중에는 WF-3·WF-4 를 배포하지 않는다

승인 대기 실행은 `webhook-waiting/<실행ID>` 로 재개된다. 대기 중에 워크플로를 import 하면
정의가 교체되고 n8n 이 재시작되어 **재개가 실패할 수 있다.** 발행이 끝난 뒤 배포한다.
대기 여부는 `execution_entity` 에서 `status='waiting'` 로 확인한다.

급하면 `pending-approval.json` 의 `resumeUrl` 에 `&text=취소` 를 붙여 호출해 정리한 뒤 배포한다.

### 배포 시 반드시 재활성화한다

`n8n import:workflow`는 워크플로를 **비활성화한다** (생성된 JSON이 `active: false`이기 때문).
스케줄러가 조용히 꺼진 채로 남으면 다음 발행일에 아무것도 오지 않는다.

```
docker exec n8n n8n import:workflow --input=/data/hooni_speed/workflows/<파일>.json
docker exec n8n n8n update:workflow --id=<워크플로ID> --active=true
docker restart n8n
```

배포 후 DB에서 `active`를 확인하고 끝낸다. 상시 활성이어야 하는 것은
`HooniWF1Sched001` · `HooniWF2Rndr0001` · `HooniWF3Appr0001` · `HooniWF4Publ0001` ·
`HooniWF6Meme00001` · `HooniWF7TgList001` · `HooniWFErr00001` 이다.


956MB · 2코어 서버라 다음을 지켜야 한다.

- 렌더링 중에는 워크플로 배포·import를 걸지 않는다. 스왑이 폭주해 VM이 마비된다.
- browserless 작업 제한은 요청 쿼리로 300초를 준다 (기본 120초로는 카드 한 장을 못 끝낸다).
- n8n HTTP 노드는 **입력 아이템 수만큼 반복 실행**된다. API가 배열을 돌려주면 다음 노드가
  20~60회 호출되어 레이트리밋에 걸린다. 모든 HTTP 노드에 `executeOnce`가 필요하다.
- 카드 한 장 렌더에 40~70초가 걸린다 (카드 종류에 따라 다르다). 2026-08 실측으로 3장짜리
  관전 가이드가 약 2분, 5장짜리 프리뷰가 약 6분이다. 7장짜리 총정리는 10분 이상 잡는다.
