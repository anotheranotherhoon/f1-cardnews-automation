# AI에게 자동화를 맡겼더니 서버가 죽었다

> 956MB 서버로 F1 카드뉴스 자동 발행하기

F1 카드뉴스를 매일 자동으로 인스타그램에 올리는 시스템을 만들었다. n8n 워크플로 7개,
HTML 템플릿 17개, AI 생성 캐릭터 68장. 코드는 거의 전부 AI 에이전트(Claude Code)가 썼다.

결과물은 만족스럽다. 하지만 "AI한테 시켰더니 알아서 다 해줬다"는 이야기는 아니다.
8일 동안 내가 한 일의 대부분은 **AI가 틀린 걸 잡아내는 것**이었다.

이 글은 그 과정에 대한 기록이다.

---

## AI가 압도적으로 잘한 것

먼저 공정하게 말하면, 이 프로젝트는 AI 없이는 이 기간에 못 끝냈다.

### 보일러플레이트 생성

n8n 워크플로는 JSON이다. 노드 하나가 20~30줄이고, 노드가 55개면 JSON이 4천 줄이 넘는다.
이걸 손으로 쓸 수는 없으니 생성기를 만들어야 하는데, AI는 이런 걸 몇 분 만에 짠다.

```js
function http(name, url, pos) {
  const extra = { executeOnce: true };
  if (url.includes('openf1')) Object.assign(extra, { retryOnFail: true, maxTries: 5 });
  return n(name, 'httpRequest', { url, options: { timeout: 25000 } }, pos, 4.2, extra);
}
```

이런 헬퍼를 만들고 55개 노드를 찍어내는 작업은 사람이 하면 지루하고 오타가 난다.
AI는 지루해하지 않는다.

### 도메인 API 탐색

Jolpica-F1과 OpenF1은 처음 써보는 API였다. 문서를 읽고, 응답 구조를 파악하고, 필요한
필드를 뽑아내는 과정을 AI가 대신 했다.

```bash
curl -s "https://api.jolpi.ca/ergast/f1/2026/11/results.json" | ...
```

이런 탐색을 수십 번 반복하면서 "이 필드는 `Lapped`일 수도 있다" 같은 걸 찾아냈다.
내가 직접 했으면 반나절은 걸렸을 것이다.

### CSS 레이아웃

카드는 HTML/CSS로 만들어 Chromium 스크린샷을 찍는 방식이다. 1080×1350 안에 22명 순위를
2열로 넣고, 넘치지 않게 행 높이를 계산하는 일 — AI가 잘한다.

```
행 높이 = padding 16 + bar 32 = 48px
11행 = 48*11 + gap 8*10 = 608px
헤더 203 + 본문 608 + 푸터 60 + 여백 144 = 1015px  → 1350 안에 들어감
```

이런 계산을 매번 해가며 템플릿을 만들었다. 사람보다 빠르고 정확했다.

---

## AI가 반복적으로 틀린 것

여기서부터가 이 글의 본론이다.

### 원인을 확신에 차서 틀리게 말한다

서버(956MB OCI 프리티어)가 하루 동안 완전히 멈춘 적이 있다. 원인을 찾는 과정에서 AI는
이렇게 말했다.

> "타임아웃 문제입니다."
> "n8n의 파일 쓰기 노드가 원인입니다."
> "실행 데이터가 누적돼서 그렇습니다."

셋 다 틀렸다. 실제 원인은 **스왑이 0**이었다. `swapon --show` 한 줄이면 나올 것이었다.

문제는 틀렸다는 게 아니라 **말투가 매번 확신에 차 있었다**는 것이다. "아마도"나 "확인이
필요하지만" 없이 단정적으로 말한다. 그래서 나는 세 번이나 잘못된 방향으로 시간을 썼다.

결국 내가 개입했다.

> "서버는 뭐가 문제냐"

이렇게 물으니 그제서야 측정을 시작했고 실제 원인이 나왔다. 이후로는 AI가 원인을 말하면
**"그거 확인해본 거냐, 짐작이냐"**를 먼저 물어보게 됐다.

같은 패턴이 API 레이트 리밋에서도 반복됐다.

```
AI: "초당 제한입니다" → 재시도 간격 늘림 → 실패
AI: "누적 호출입니다" → 시간 두고 재실행 → 실패
나: (측정해보라고 요구)
AI: 서버에서 6번 연속 호출 → 전부 성공 → "제 가설이 틀렸습니다"
```

진짜 원인은 n8n의 동작 방식이었다. **HTTP 노드는 입력 아이템 수만큼 반복 실행된다.**
API가 배열 22개를 돌려주면 다음 노드가 22번 호출된다. 문서에 있는 내용이지만, AI도
겪기 전엔 몰랐다.

### 운영 중인 것을 건드린다

AI가 두 번이나 **실행 중인 작업의 파일을 삭제**했다.

```
[3] chrome=0   ← 렌더링 끝났다고 판단
     정리      ← 폴더 삭제
                  그런데 그 직후 텔레그램 전송 단계에서 404 → 실패
```

Chromium 프로세스가 0개가 되는 시점은 **렌더링** 종료지 **실행** 종료가 아니다. 그 뒤에
저장 → 전송 → 승인 대기가 남아 있다. AI는 이 구분을 못 했고, 같은 실수를 두 번 했다.

코드를 짜는 능력과 **운영 중인 시스템을 다루는 감각**은 다른 문제다. 후자는 "이걸 지우면
지금 뭐가 깨질 수 있나"를 자동으로 떠올리는 일인데, AI에게는 그 반사신경이 없었다.

### 맥락을 잃는다

대화가 길어지면 AI는 앞서 정한 것을 잊는다. 예를 들어 "순위 카드는 1등부터 꼴등까지
전부 보여준다"는 규칙을 정했는데, 월요일 카드만 고치고 스프린트 카드는 그대로 뒀다.

```
월요일 레이스 결과      22명 ✅
스프린트 결과          10명 ❌  ← 같은 규칙인데 누락
드라이버 순위(스프린트)  10명 ❌
```

내가 발견해서 지적할 때까지 몰랐다. 그것도 여러 번.

가장 아팠던 건 **텔레그램으로 보낸 수정 요청 두 건이 통째로 사라진 것**이다. 승인 화면에서
보낸 수정은 그 실행에만 적용되는 1회성인데, AI가 새 실행을 걸면서 날아갔다. 복구할 방법도
없었다.

이 일이 있고 나서 내가 요구했다.

> "기록을 해야 하는 거 아니냐? 다음에는 이러면 안 되잖아. 템플릿이 문제인 거 아니냐"

그래서 `CARD-SPEC.md`라는 명세 문서를 만들었다.

```markdown
**이 문서가 단일 출처다.** 카드 구성·문구 규칙을 바꿀 때는 여기를 먼저 고치고,
그다음 build-wf1.js와 templates/에 반영한다.

텔레그램으로 보내는 수정 요청은 그 실행에만 적용되는 1회성이다.
매주 반복되어야 하는 요구사항은 반드시 이 문서에 적고 코드에 반영해야 한다.
```

**AI와 오래 일하려면 문서가 필수다.** 사람끼리는 "저번에 말했잖아"가 통하지만 AI에게는
안 통한다. 대화가 리셋되면 백지가 된다.

---

## 끝까지 내가 해야 했던 것

### 도메인 지식

AI는 F1을 모른다. 학습한 지식은 있지만 이 프로젝트에 필요한 판단은 못 한다.

**스프린트 주말 구조**가 대표적이다. 나는 스프린트 주간에 세션이 어떻게 배치되는지 알고
있었고, 그래서 요일별 레시피를 7종으로 나눠야 한다는 걸 처음부터 요구할 수 있었다.
AI는 내가 말하기 전까지 이 구조를 고려하지 않았다.

**러셀의 T포즈**도 그렇다. 캐릭터 포즈를 만들 때 내가 지정했다.

> "조지 러셀은 본인의 시그니처 포즈인 T포즈로 가야지!"

AI는 이런 걸 스스로 못 낸다. 낸다 해도 사실인지 검증할 수 없다. 실제로 AI가 다른
드라이버들의 "시그니처 포즈"를 제안하려 하길래, 내가 확실한 것만 넣자고 정리했다.
근거 없는 걸 브랜드로 굳히면 나중에 곤란해진다.

### 사실 왜곡을 잡아내는 눈

이게 가장 중요했다. 스프린트 주말 카드 표지가 이렇게 나왔다.

```
British Grand Prix
스프린트 & 퀄리파잉
우승 Andrea Kimi Antonelli
```

내가 물었다.

> "여기서 스프린트&퀄리파잉이 둘째 줄에 나오고 그다음 줄에 우승자가 나오니까
> 퀄리파잉도 1등했다는 거야 뭐야? 의미가 혼동됨"

AI는 이걸 문제로 인식하지 못하고 있었다. 마침 그 경기는 안토넬리가 둘 다 가져가서
우연히 맞았지만, 다른 사람이 폴이었다면 **틀린 정보가 그대로 발행**됐을 것이다.

```
British Grand Prix
스프린트 우승 Andrea Kimi Antonelli
폴 포지션 Lewis Hamilton
```

한 줄에 세션 이름과 그 결과를 함께 담는 규칙으로 정리했다.

자동 발행 계정에서 틀린 정보 하나는 사람이 실수한 것보다 무겁다. 매일 자동으로 나가니까.
**이 감각은 AI에게 위임할 수 없었다.**

### UX 방향

수정 방식을 세 번 갈아엎었는데, 매번 방향을 낸 건 나였다.

처음엔 텔레그램 채팅에 "카드 1번 수정해줘" 식으로 입력하는 방식이었다. 써보니 불편했다.

> "카드 1, 카드 2 이런 식으로 작성하니 내가 어떤 요소를 수정하라는지 너가 잘 이해하지
> 못하는 것 같애"

그래서 요구했다.

> "이미 카드 1, 카드 2에 대한 수정사항을 입력하는 창이 있고, 내가 해당 영역을 채워서
> 제출하는 구조로 하고 싶다고"

AI는 이 요구를 받고 나서야 편집 페이지를 만들었다. 만들고 나니 좋았고, 여기에 하나 더
얹었다.

> "그냥 내가 자연어로 지시를 하고 싶을 때도 있을 것 같애, 그럴 때를 위해서 코멘트하는
> 영역도 추가해야할듯. 물론 그 코멘트도 카드마다 개별적인 것 하나씩, 전체 코멘트 하나"

정확한 문구를 아는 곳은 직접 입력하고, 느낌만 있는 곳은 자연어로 적는다. 코멘트가
하나라도 있을 때만 LLM을 거치니 대부분의 수정은 0원에 즉시 반영된다.

**"뭐가 불편한지"는 써본 사람만 안다.** AI는 내가 불편하다고 말하기 전까지 몰랐다.

### 미적 판단

레이스 그리드를 처음엔 일렬로 나열했다. 내가 요구했다.

> "실제 F1 스타팅 그리드처럼 배치해줘. 1번 왼쪽, 2번 오른쪽, 3번 왼쪽... 지그재그 형태로"

이건 데이터가 같아도 **보여주는 방식이 정보를 더한다**는 판단이다. F1을 아는 사람이라면
지그재그 배치만 봐도 "아 그리드구나" 하고 안다.

컨스트럭터 순위도 마찬가지다. AI는 22명 드라이버와 똑같이 2열로 만들었는데, 팀은 11개뿐이라
한 열로 충분했다. 내가 지적하고 나서 고쳤다.

---

## AI가 AI를 다룰 때도 같은 문제가 있었다

재미있는 관찰. 이 시스템은 Gemini로 이미지와 텍스트를 생성하는데, 그 **프롬프트를 AI가
작성**한다. AI가 AI에게 지시하는 구조다.

여기서도 같은 실패가 나왔다.

### 프롬프트 설계 오류

이슈 요약이 자꾸 비어서 나왔다. 원인은 AI가 쓴 프롬프트 마지막 문장이었다.

```
"확실하지 않은 정보는 null."
```

개별 필드를 비우라는 의도였는데, Gemini는 **응답 전체를 `null`로** 반환했다.

```json
{ "parts": [{ "text": "null" }] }
```

사람이 읽으면 의도가 명확한 문장인데, 모델은 다르게 읽었다. 고친 문구는 이렇다.

```
반드시 최상위는 위 키들을 가진 JSON 객체여야 한다 —
응답 전체를 null로 출력하지 말 것.
못 찾은 개별 필드만 null, issues를 못 찾으면 빈 배열 []로 둘 것.
```

### 금지만 반복하다 실패

드라이버 캐릭터를 그릴 때 스폰서 로고가 계속 따라 그려졌다. AI의 대응은 이랬다.

```
1차  "ZERO logos, ZERO sponsor patches" 강조        실패
2차  어깨·가슴 콕 집어 금지                          실패
3차  "leaping cat" 명시적 금지                       실패
4차  로고 없는 캐릭터를 참조 이미지로 사용             실패
```

네 번 모두 **금지를 강화하는 방향**이었다. 다섯 번째에야 접근을 바꿨다.

```
CHEST DESIGN (fills the space so there is no empty panel):
대각선 스트라이프 하나를 가슴에 넣는다. 이것이 유일한 장식이며 로고를 대체한다.
```

빈 공간을 먼저 채우니 로고가 들어갈 자리가 없어졌다. 15장 중 9장이 완전히 깨끗해졌다.

**금지보다 대체재가 효과적이다.** 이건 사람에게 지시할 때도 비슷하지 않나 싶다.

---

## 그래서 어떻게 일해야 하나

8일간 얻은 실무적인 결론이다.

### 1. AI의 진단은 가설로 취급한다

"원인은 X입니다"라고 하면 "확인했냐"고 되묻는다. 확인 안 했으면 확인부터 시킨다.
이 습관 하나로 헛수고가 크게 줄었다.

### 2. 운영 중인 자원은 사람이 관리한다

배포·삭제·재시작처럼 되돌리기 어려운 작업은 AI에게 자동으로 맡기지 않는다.
"지금 뭐가 돌고 있나"를 먼저 확인하게 하고, 확인 결과를 내가 본다.

### 3. 요구사항은 코드가 아니라 문서에 먼저 적는다

대화는 휘발된다. 명세 문서를 만들고 "여기에 먼저 적고 코드에 반영하라"는 규칙을 세우니
같은 요구사항을 반복해서 설명하는 일이 사라졌다.

여기에 **밟았던 지뢰도 함께 적었다.**

```markdown
| 증상 | 확인할 것 |
|---|---|
| 이슈가 빔 | last-research.json — LLM 응답 원문과 실패 이유 |
| 승인 요청이 안 옴 | pending-approval.json의 **갱신 시각** (존재 여부 아님) |
| 반영 안 된 것처럼 보임 | 텔레그램의 🆔 식별자로 세트 구분 |
```

두 번째 줄은 AI가 실제로 여러 번 틀린 것이다. 파일 존재 여부로 판단해서 이전 실행 것을
새 것으로 착각했다.

### 4. 검증은 결국 내 몫이다

AI가 만든 걸 내가 다 봐야 한다. 카드가 나오면 눈으로 확인하고, 숫자가 맞는지 보고,
문장이 오해를 부르지 않는지 읽는다.

이 프로젝트에서 내가 잡아낸 것들이다.

```
스프린트 결과가 10위까지만 나옴
컨스트럭터 순위 팀 색깔이 전부 회색
Q1·Q2 정보가 아예 없음
표지 문구가 두 줄로 넘쳐서 이름이 잘림
"스프린트 & 퀄리파잉 / 우승 X" 가 사실 왜곡 소지
```

전부 AI가 "완료했습니다"라고 보고한 뒤에 발견한 것들이다.

---

## 숫자로 본 결과

```
기간        8일
워크플로     7개 (노드 55개)
템플릿      17개 HTML
캐릭터      기본 22명 + 액션 포즈 46장
```

```
제작 비용    약 6,000원  (AI 이미지 생성, 일회성)
운영 비용    주 110원 → 연 약 3,000원
서버        기존 OCI 프리티어 재사용 (추가 비용 0원)
```

첫 자동 발행까지 **렌더링 실패 7회**를 겪었다. 원인은 매번 달랐고, 그중 절반은 AI가
만든 것이었다.

---

## 정리하며

AI 에이전트와 일하는 건 **주니어 개발자 여러 명을 동시에 부리는 것**과 비슷했다.

코드는 빠르고 많이 나온다. 지루한 일을 마다하지 않는다. 모르는 API도 금방 파악한다.
하지만 자기가 틀렸다는 걸 모르고, 운영 중인 시스템을 조심스럽게 다룰 줄 모르고,
어제 정한 걸 오늘 잊는다.

그래서 내 역할이 바뀌었다. 코드를 쓰는 대신 **방향을 정하고, 결과를 검증하고, 사실관계를
지키는 사람**이 됐다. 일이 줄었냐면 그건 아니다. 종류가 달라졌다.

특히 마지막 하나 — **사실관계를 지키는 일**은 앞으로도 위임할 수 없을 것 같다.
매일 자동으로 콘텐츠가 나가는 시스템에서, "이 문장이 오해를 부르지 않나"를 묻는 건
결국 사람이 해야 하는 일이었다.

첫 자동 발행은 이번 주 목요일이다.
